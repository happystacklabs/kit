import React from 'react';
import ActionList from ".."
import {shallow, mount} from 'enzyme';


describe('ActionList', () => {
  describe('sections', () => {
    it('', () => {
      expect(true).toBe(true);
    });
  });
  describe('position', () => {
  });
});
