import React, { Component } from 'react';
import PropTypes from 'prop-types';
import './Icon.css';

class Icon extends Component {
  static propTypes = {
  };

  static defaultProps = {
   };

  render() {
    return (

    );
  }
}

export default Icon;
